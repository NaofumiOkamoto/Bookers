
  <h2>Editing book</h2>

  <%= form_for(@book) do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title %>

    <h4>Body</h4>
    <%= f.text_area :body %>
    <p><%= f.submit'Update Book' %></p>

  <% end %>
<div class="show_edit_back">
   <p><%= link_to 'Show', book_path %> |<%= link_to 'Back', books_path %></p>
</div>
